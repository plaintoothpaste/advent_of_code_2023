#pragma once
#include <vector>
// just some basic type alias
using field_t = std::vector<std::vector<size_t>>;

using pair_single_t = std::pair<size_t,size_t>;
using pair_t = std::vector<pair_single_t>;
